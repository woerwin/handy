// pageTransition 的单元测试
define(function (require){
    var PageTransition = require('../src/pageTransition'),
        instance;

    describe('pageTransition',function (){
        beforEach(function (){
            instance = new PageTransition({

            });
        });

        it('should be has render,getPage and transition method.',function (){

        });

        it('should be has options property.',function (){

        });
    });
});